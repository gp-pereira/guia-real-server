// If you want to replace sequelize for another storage system,
// you just have to create a interface similar to 'sequelize/index.js'

// Make sure to run the integration tests after the swap

module.exports = require("./sequelize");